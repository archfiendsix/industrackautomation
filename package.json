{
  "name": "industrack",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx cypress run --browser chrome",
    "test-per-spec": "npx cypress run --browser chrome --spec \"cypress/e2e/newTaxAndTerms.cy.js\"",
    "merge-report": "mochawesome-merge cypress/report/mochawesome-report/*.json > cypress/report/index.json && marge cypress/report/index.json --reportDir ./cypress/report/ --inline"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "allure-commandline": "^2.18.1",
    "cypress": "^10.3.1",
    "cypress-plugin-tab": "^1.0.5",
    "mocha-allure-reporter": "^1.4.0",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.2.1"
  },
  "devDependencies": {
    "cypress-plugin-tab": "^1.0.5"
  }
}